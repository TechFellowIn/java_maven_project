apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: search-data
  name: search-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
